@model CMCSApplication.Models.Claim

@{
    ViewData["Title"] = "Submit Claim";
}

<div class="container py-4">
    <h1 class="mb-3">Submit Claim</h1>
    <p class="text-muted">Fill out the form below to submit your monthly claim.</p>

    <!-- Alerts -->
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }
    @if (TempData["UploadMessage"] != null)
    {
        <div class="alert alert-info alert-dismissible fade show" role="alert">
            @TempData["UploadMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <div class="card shadow-sm">
        <div class="card-body">
            <form asp-action="Submit" asp-controller="Lecturer" method="post" enctype="multipart/form-data">
                <div class="row g-3">

                    <!-- Lecturer Name (READ ONLY) -->
                    <div class="col-md-6">
                        <label class="form-label">Lecturer Name</label>
                        <input class="form-control" value="@Model.LecturerName" readonly />
                    </div>

                    <!-- Department (editable) -->
                    <div class="col-md-6">
                        <label asp-for="Department" class="form-label"></label>
                        <input asp-for="Department" class="form-control" placeholder="e.g., Computer Science" />
                        <span asp-validation-for="Department" class="text-danger"></span>
                    </div>

                    <!-- Month -->
                    <div class="col-md-4">
                        <label asp-for="Month" class="form-label"></label>
                        <input asp-for="Month" class="form-control" placeholder="e.g., October 2025" />
                        <span asp-validation-for="Month" class="text-danger"></span>
                    </div>

                    <!-- Hours Worked -->
                    <div class="col-md-4">
                        <label asp-for="HoursWorked" class="form-label"></label>
                        <input asp-for="HoursWorked" class="form-control" placeholder="e.g., 24" />
                        <span asp-validation-for="HoursWorked" class="text-danger"></span>
                    </div>

                    <!-- Hourly Rate (READ ONLY - pulled from Lecturer) -->
                    <div class="col-md-4">
                        <label class="form-label">Hourly Rate (Auto)</label>
                        <input class="form-control" value="@Model.HourlyRate" readonly />
                    </div>

                    <!-- File Upload -->
                    <div class="col-md-12">
                        <label asp-for="UploadFile" class="form-label">Supporting Document (PDF, DOCX, XLSX)</label>
                        <input asp-for="UploadFile" type="file" class="form-control" accept=".pdf,.docx,.xlsx" />
                        <span asp-validation-for="UploadFile" class="text-danger"></span>
                    </div>

                    <!-- Notes -->
                    <div class="col-12">
                        <label asp-for="Notes" class="form-label"></label>
                        <textarea asp-for="Notes" class="form-control" rows="3" placeholder="Optional notes..."></textarea>
                        <span asp-validation-for="Notes" class="text-danger"></span>
                    </div>

                </div>

                <div class="mt-4 d-flex gap-2">
                    <button type="submit" class="btn btn-success px-4">Submit</button>
                    <a class="btn btn-outline-secondary px-4" asp-controller="Lecturer" asp-action="Index">Back</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
